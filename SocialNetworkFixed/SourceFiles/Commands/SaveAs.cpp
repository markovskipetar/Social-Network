#include "../../Headers/Commands/SaveAs.h"

void SaveAs::execute() const
{
	/*char fileName[128];
	std::cin.getline(fileName, 128);

	std::ofstream fileWriter(fileName);

	socialNetwork->write(fileWriter);

	fileWriter.flush();
	fileWriter.close();*/
}
